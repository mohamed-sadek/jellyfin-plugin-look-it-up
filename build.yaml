---
name: "Look it up"
guid: "a8ab0fed-cac9-406d-b98b-58161bf970b8"
version: "1.2.56.0"
targetAbi: "10.11.0.0"
framework: "net9.0"
overview: "Timed subtitle popups via Wikipedia + Wikidata (optional Groq complement)"
description: >
  Prepares timed explainer popups from subtitle names using Wikipedia search and Wikidata
  types (no API key). Groq is optional for ambiguous pages, popup rewrites, and leftover idioms.
category: "General"
owner: "lookitup"
artifacts:
  - "Jellyfin.Plugin.LookItUp.dll"
changelog: >
  Default prepare uses Wikipedia search + Wikidata types (no API key). Groq is optional:
  tie-break ambiguous pages, rewrite popups for non-US viewers, and scan leftover cues
  for uncapitalized US idioms. Also persist sidecar early and extract possessive heads
  (Jon Voight from "Jon Voight's LeBaron").
