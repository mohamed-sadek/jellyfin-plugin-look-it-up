---
name: "Look it up"
guid: "a8ab0fed-cac9-406d-b98b-58161bf970b8"
version: "1.2.58.0"
targetAbi: "10.11.0.0"
framework: "net9.0"
overview: "Timed subtitle popups via Wikipedia + Wikidata (optional Groq complement)"
description: >
  Prepares timed explainer popups from subtitle names using Wikipedia search and Wikidata
  types (no API key). Groq is optional for ambiguous pages, popup rewrites, and leftover idioms.
category: "General"
owner: "lookitup"
artifacts:
  - "Jellyfin.Plugin.LookItUp.dll"
changelog: >
  Groq can no longer keep a page Wikipedia did not find. Skip quoted filler ("yes, yes"),
  require a Wikipedia URL for idiom popups, and use the next capitalized cue word as search
  extra (Consumer + Volvo → Consumer Reports).
