---
name: "Look it up"
guid: "a8ab0fed-cac9-406d-b98b-58161bf970b8"
version: "1.2.57.0"
targetAbi: "10.11.0.0"
framework: "net9.0"
overview: "Timed subtitle popups via Wikipedia + Wikidata (optional Groq complement)"
description: >
  Prepares timed explainer popups from subtitle names using Wikipedia search and Wikidata
  types (no API key). Groq is optional for ambiguous pages, popup rewrites, and leftover idioms.
category: "General"
owner: "lookitup"
artifacts:
  - "Jellyfin.Plugin.LookItUp.dll"
changelog: >
  Reject Wikipedia pages whose titles do not match the subtitle term. Do not treat episode
  guest stars as in-show cast. Use the following word and the previous cue to disambiguate
  namesakes; skip greetings, demonyms, and quoted titles with no matching article.
