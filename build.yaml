---
name: "Look it up"
guid: "a8ab0fed-cac9-406d-b98b-58161bf970b8"
version: "1.2.60.0"
targetAbi: "10.11.0.0"
framework: "net9.0"
overview: "Timed subtitle popups from a chat model (Groq/OpenAI/Ollama)"
description: >
  Finds capitalized names in subtitles, then batches those names to Groq (or OpenAI/Ollama).
  The model explains US culture a non-native with some knowledge would still miss (Roosevelt),
  and skips states, money, and filler. Popup appearance survives plugin updates.
category: "General"
owner: "lookitup"
artifacts:
  - "Jellyfin.Plugin.LookItUp.dll"
changelog: >
  Find names locally before Groq. Skip US states, money, and filler in code.
  Prompt for a non-native with some US knowledge (explain Roosevelt, not Missouri).
  Re-prepare items (cache v21).
